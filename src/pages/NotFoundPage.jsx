import React from "react";
import EmptyState from "../components/EmptyState.jsx";

export default function NotFoundPage() {
  return <EmptyState title="Страница не найдена" desc="Похоже, ты перешёл по неверной ссылке." />;
}